version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data          # SQLite database and backups persist here
    env_file: ./backend/.env
    restart: unless-stopped
